Let's start building all idea one by one. i need all the functionality available with other isometric tools but let's divide the idea and start with small pieces. So, Idea is to 
- create a symbol in each seperate python file and if user want's to put their own symbol they can create more symbol. This python file will have certain parameters as input and it will place symbol accordingly in isometric drawings with dimensions.
- Output should be in pdf, svg and any cad supportive formate e.g. step, etc 

so for this purpose first give me folder structure for generating my pip package with conda package


Repo name & Package name- OPE-IsoGen
Default: pythonocc-core

Python version Latest and compatible with pip

Plugin style for symbols should be discovered based on configuration txt file available in any specific path. and when they import the package, user should be able to add extra folder in python for discovering the files such that debugging will become easy for users.


start with Pipe Data file.txt as input -> SVG →  then CAd Format & PDFs whichever format is selected

CLI Should have following functionality:
1. Option for choosing the configuration file.
2. Generate file based on output type
3. Type of operation we are asking e.g. Data load, isoGenerate, Data process... basically these operation type we will be removing based on future expansion but currently we don''t have anything and to test our code we need extensibility
4. Piping Datafile selection

For the first stage just give me only very necessory folder structure for project e.g. only conda and pip package generation related setup not more. if you think some file is necessory give me that too.


now my idea is to create a 2D Drawing Symbol inside the each python file such that it will be projected to any one of the Plane e.g. XY or YZ or ZX during runtime and it will be rotated based on the requirement in isometric.
Additionally each symbol file will have connection points array mandatory which will hold the points from where next symbol will start detailing.

e.g. elbo has 2 points 1 input 2 output. and output will be connected to some other element etc.

so now my idea is to 
1. first write the code for elbo and straight pipe then 
2. i am thinking about testing the elbo connection 
3. additing pipe and elbo with marking dimensions.

if you have any question ask me else let's built it step by step and ask me questions if you are not clear.


100  1001=100  1004=400 10001=Pt0_X 10002=Pt0_y 10003=Pt0_z 10004=StartPt_X 10005=StartPt_y 10006=StartPt_z 10007=EndPt_X 10008=EndPt_y 10009=EndPt_z
